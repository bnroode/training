<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:XLabs.Forms.Controls;assembly=XLabs.Forms"
             x:Class="Training.View.CameraView">
  <StackLayout>
    <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
    <Button Text="Take a picture" Command="{Binding TakePictureCommand}" VerticalOptions="Start" HorizontalOptions="Center"></Button>
    <Button Text="Select a picture" Command="{Binding SelectPictureCommand}" VerticalOptions="StartAndExpand" HorizontalOptions="Center"></Button>
    <Image Source="{Binding ImageSource}" VerticalOptions="CenterAndExpand" />
  </StackLayout>
</ContentPage>